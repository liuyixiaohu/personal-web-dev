name: Fetch Jobs

on:
  schedule:
    # 06:00 UTC — after Tesla (2 AM UTC) and NPM (4 AM UTC) monitors complete
    - cron: '0 6 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install requests>=2.31.0

      - name: Fetch jobs from monitor repos
        run: python scripts/fetch_jobs.py
        env:
          GITHUB_TOKEN: ${{ secrets.JOBS_PAT }}

      - name: Commit updated job data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add public/data/jobs.json
          git diff --cached --quiet || git commit -m "chore: update job data $(date -u +%Y-%m-%d)"
          git push
