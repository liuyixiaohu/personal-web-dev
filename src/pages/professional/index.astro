---
import BaseLayout from '../../layouts/BaseLayout.astro';
import BarScene from '../../components/bar/BarScene.svelte';
---

<BaseLayout title="Professional — Likun" description="Likun's professional expertise — Product Marketing, Data Science, and Visual Design.">
  <main class="professional-page">
    <h1 class="sr-only">Professional</h1>
    <a href="/" class="back-link" data-i18n="nav.back">&larr; Back</a>
    <BarScene client:only="svelte" />
  </main>
</BaseLayout>

<style>
  .professional-page {
    position: relative;
    min-height: 100dvh;
    overflow: hidden;
  }

  .back-link {
    position: fixed;
    top: var(--space-md);
    left: var(--space-md);
    font-size: var(--fs-sm);
    color: var(--text-light);
    z-index: 10;
  }

  .back-link:hover {
    color: var(--text);
    opacity: 1;
  }
</style>
