---
import BaseLayout from '../layouts/BaseLayout.astro';
import GlobeScene from '../components/globe/GlobeScene.svelte';
---

<BaseLayout title="Life Journey — Kun Li" description="Explore Kun's life journey through an interactive 3D globe — cities, stories, and experiences from around the world.">
  <main class="journey-page">
    <h1 class="sr-only">Life Journey</h1>
    <a href="/" class="back-link" data-i18n="nav.back">&larr; Back</a>
    <GlobeScene client:only="svelte" />
  </main>
</BaseLayout>

<style>
  .journey-page {
    position: relative;
    min-height: 100dvh;
    overflow: hidden;
  }

  .back-link {
    position: fixed;
    top: var(--space-md);
    left: var(--space-md);
    font-size: var(--fs-sm);
    color: var(--text-light);
    z-index: 10;
  }

  .back-link:hover {
    color: var(--text);
    opacity: 1;
  }
</style>
