---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="Sift" description="Clean up your LinkedIn feed. Hide ads, filter spam, mute keywords, and unfollow with one click.">
  <main class="jl-page grid-page">
    <a href="/" class="back-link" data-i18n="nav.back">&larr; Back</a>

    <!-- Header -->
    <header class="jl-header">
      <img src="/images/joblens-icon.png" alt="Sift icon" class="jl-logo" />
      <div>
        <div class="jl-title-row">
          <h1>Sift</h1>
          <div class="jl-version-wrap">
            <button class="jl-version-btn" id="jl-changelog-toggle" aria-label="View changelog">v2.1</button>
            <div class="jl-changelog-backdrop" id="jl-changelog-backdrop" style="display:none"></div>
            <div class="jl-changelog-popup" id="jl-changelog-popup" style="display:none">
              <div class="jl-changelog-header">
                <span class="jl-changelog-title" data-i18n="joblens.changelogTitle">Changelog</span>
                <button class="jl-changelog-close" id="jl-changelog-close" aria-label="Close changelog">&times;</button>
              </div>
              <div id="jl-changelog-content"></div>
            </div>
          </div>
          <a class="jl-github-btn" href="https://github.com/liuyixiaohu/sift" target="_blank" rel="noopener" aria-label="View on GitHub">GitHub</a>
          <span class="jl-feedback-wrap">
            <button class="jl-feedback-btn" id="jl-feedback-toggle" data-i18n="joblens.feedback" aria-label="Send feedback">Feedback</button>
            <div class="jl-feedback-backdrop" id="jl-feedback-backdrop" style="display:none"></div>
            <div class="jl-feedback-popup" id="jl-feedback-popup" style="display:none">
              <div class="jl-feedback-header">
                <span class="jl-feedback-title" data-i18n="joblens.feedbackTitle">Any Suggestion? Pls!!!</span>
                <button class="jl-feedback-close" id="jl-feedback-close" aria-label="Close feedback form">&times;</button>
              </div>
              <form id="jl-feedback-form">
                <textarea name="message" required rows="4" class="jl-feedback-textarea" id="jl-feedback-textarea" placeholder="Bugs, ideas, anything..."></textarea>
                <input type="text" name="_gotcha" style="display:none" tabindex="-1" autocomplete="off" />
                <button type="submit" class="jl-feedback-submit" id="jl-feedback-submit" data-i18n="joblens.feedbackSend">Send</button>
              </form>
              <p class="jl-feedback-success" id="jl-feedback-success" style="display:none" data-i18n="joblens.feedbackSent">Thanks! Feedback sent.</p>
              <p class="jl-feedback-error" id="jl-feedback-error" style="display:none" data-i18n="joblens.feedbackError">Failed to send. Try again?</p>
            </div>
          </span>
        </div>
        <p class="jl-subtitle" data-i18n="joblens.subtitle">
          Take back a LinkedIn worth your time.
        </p>
      </div>
    </header>

    <!-- Inspiration -->
    <section class="section">
      <h2 data-i18n="joblens.inspirationTitle">Inspiration</h2>
      <p data-i18n="joblens.inspiration1">LinkedIn is great at surfacing relevant jobs and content. But the experience? Ads everywhere, "Suggested" posts from strangers, reposted job listings, no way to tell if a role sponsors visas without clicking through. Your feed is half noise, and your job search is mostly triage.</p>
      <p data-i18n="joblens.inspiration2">Sift exists because the platform is good but the workflow is broken. It cleans up both sides of LinkedIn: your feed and your job search.</p>
    </section>

    <!-- Feed Cleanup -->
    <section class="section">
      <h2 data-i18n="joblens.feedTitle">Feed Cleanup</h2>
      <p data-i18n="joblens.feedDesc">Sift automatically hides ads, suggested posts, and recommended content from your LinkedIn feed. Mute and Unfollow buttons appear inline on every post. Search your keyword rules right from the popup.</p>
      <ul class="jl-features">
        <li data-i18n="joblens.feat.hideAds"><strong>Hide Ads</strong>: removes Promoted posts automatically</li>
        <li data-i18n="joblens.feat.hideSuggested"><strong>Hide Suggested & Recommended</strong>: filters non-connection noise</li>
        <li data-i18n="joblens.feat.muteKeywords"><strong>Mute Keywords</strong>: add keywords via popup or right-click menu</li>
        <li data-i18n="joblens.feat.unfollow"><strong>Inline Mute & Unfollow</strong>: buttons on every post — no hunting for the ... menu</li>
        <li data-i18n="joblens.feat.search"><strong>Search Rules</strong>: find keywords quickly in the popup</li>
        <li data-i18n="joblens.feat.forceRecent"><strong>Force Recent</strong>: switch to chronological sort automatically</li>
        <li data-i18n="joblens.feat.hideSidebar"><strong>Hide Sidebar</strong>: remove LinkedIn News and footer clutter</li>
        <li data-i18n="joblens.feat.pause"><strong>Shift+J</strong>: pause/resume all filters instantly</li>
      </ul>
    </section>

    <!-- Job Search -->
    <section class="section">
      <h2 data-i18n="joblens.jobsTitle">Job Search Intelligence</h2>
      <p data-i18n="joblens.jobsDesc">On LinkedIn's job search results page, Sift flags suspicious listings so you skip the ones not worth your time.</p>
      <div class="jl-flags">
        <div class="jl-flag">
          <span class="jl-flag-badge" style="background:#D9797B">Reposted</span>
          <span class="jl-flag-desc" data-i18n="joblens.flag.reposted">Job has been reposted — stale listing</span>
        </div>
        <div class="jl-flag">
          <span class="jl-flag-badge" style="background:#D9797B">Applied</span>
          <span class="jl-flag-desc" data-i18n="joblens.flag.applied">You already applied to this one</span>
        </div>
        <div class="jl-flag">
          <span class="jl-flag-badge" style="background:#D9797B">No Sponsor</span>
          <span class="jl-flag-desc" data-i18n="joblens.flag.noSponsor">Description says visa sponsorship not available</span>
        </div>
        <div class="jl-flag">
          <span class="jl-flag-badge" style="background:#D9797B">Unpaid</span>
          <span class="jl-flag-desc" data-i18n="joblens.flag.unpaid">Volunteer or unpaid position</span>
        </div>
        <div class="jl-flag">
          <span class="jl-flag-badge" style="background:#D9797B">Skipped Co.</span>
          <span class="jl-flag-desc" data-i18n="joblens.flag.skippedCo">Company on your skip list</span>
        </div>
        <div class="jl-flag">
          <span class="jl-flag-badge" style="background:#D9797B">Skipped Title</span>
          <span class="jl-flag-desc" data-i18n="joblens.flag.skippedTitle">Title contains a keyword you're avoiding</span>
        </div>
      </div>
    </section>

    <!-- See It in Action -->
    <section class="section">
      <h2 data-i18n="joblens.demoTitle">See It in Action</h2>
      <div class="jl-screenshots">
        <figure class="jl-screenshot-figure">
          <img src="/images/joblens-dim-off.png" alt="Sift badges on LinkedIn job cards" class="jl-screenshot" />
          <figcaption data-i18n="joblens.cap.badges">Badges flag issues at a glance</figcaption>
        </figure>
        <figure class="jl-screenshot-figure">
          <img src="/images/joblens-dim-on.png" alt="Sift dim mode fading flagged cards" class="jl-screenshot" />
          <figcaption data-i18n="joblens.cap.dim">Dim mode fades flagged cards</figcaption>
        </figure>
      </div>
    </section>

    <!-- Get Started -->
    <section class="section">
      <h2 data-i18n="joblens.startTitle">Get Started</h2>
      <ol class="jl-steps">
        <li data-i18n="joblens.step1">Install from the <a href="https://chromewebstore.google.com/detail/ainpeagpaoafcblhcebbakofagiocooa?utm_source=kunli_website" class="jl-link" target="_blank" rel="noopener">Chrome Web Store</a></li>
        <li data-i18n="joblens.step2">Visit <strong>LinkedIn</strong> — your feed is automatically cleaned up</li>
        <li data-i18n="joblens.step3">Go to <strong>Jobs &rarr; Search Results</strong> — cards are auto-flagged</li>
        <li data-i18n="joblens.step4">Click the <strong>Sift icon</strong> to manage keywords, view stats, and adjust settings</li>
        <li data-i18n="joblens.step5">Use inline <strong>Mute</strong> and <strong>Unfollow</strong> buttons on any post</li>
      </ol>
    </section>

    <!-- How It Works -->
    <section class="section">
      <h2 data-i18n="joblens.howTitle">How It Works</h2>
      <div class="jl-pipeline">
        <div class="jl-step">
          <span class="jl-step-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
          </span>
          <span class="jl-step-label" data-i18n="joblens.pipe.detect">Detect</span>
        </div>
        <span class="jl-arrow">&rarr;</span>
        <div class="jl-step">
          <span class="jl-step-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4z"/></svg>
          </span>
          <span class="jl-step-label" data-i18n="joblens.pipe.badge">Badge</span>
        </div>
        <span class="jl-arrow">&rarr;</span>
        <div class="jl-step">
          <span class="jl-step-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polygon points="22 3 2 3 10 12.46 10 19 14 21 14 12.46 22 3"/></svg>
          </span>
          <span class="jl-step-label" data-i18n="joblens.pipe.filter">Filter</span>
        </div>
        <span class="jl-arrow">&rarr;</span>
        <div class="jl-step">
          <span class="jl-step-icon">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"/><circle cx="12" cy="12" r="3"/></svg>
          </span>
          <span class="jl-step-label" data-i18n="joblens.pipe.focus">Focus</span>
        </div>
      </div>
      <p data-i18n="joblens.howDesc">Reads page content directly in the browser. No external servers, no API calls. Everything runs locally. Your preferences are stored via Chrome's local storage.</p>
    </section>

    <!-- Privacy -->
    <section class="section">
      <h2 data-i18n="joblens.privacyTitle">Privacy</h2>
      <p data-i18n="joblens.privacyDesc">Sift runs entirely in your browser. No data is collected, transmitted, or stored externally. All preferences and stats stay on your device.</p>
      <a href="/sift/privacy" class="jl-link" style="font-size:var(--fs-xs)" data-i18n="joblens.privacyLink">Full privacy policy &rarr;</a>
    </section>

    <!-- CTA -->
    <div class="jl-cta">
      <a href="https://chromewebstore.google.com/detail/ainpeagpaoafcblhcebbakofagiocooa?utm_source=kunli_website" class="jl-store-link" target="_blank" rel="noopener">
        <svg class="jl-store-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><circle cx="12" cy="12" r="4"/><line x1="21.17" y1="8" x2="12" y2="8"/><line x1="3.95" y1="6.06" x2="8.54" y2="14"/><line x1="10.88" y1="21.94" x2="15.46" y2="14"/></svg>
        <span>Chrome Web Store</span>
      </a>
    </div>

    <!-- Lightbox -->
    <dialog class="jl-lightbox" id="jl-lightbox">
      <img id="jl-lightbox-img" alt="" />
    </dialog>

  </main>
</BaseLayout>

<script>
  document.addEventListener('astro:page-load', () => {
    // --- Lightbox ---
    const dialog = document.getElementById('jl-lightbox') as HTMLDialogElement | null;
    const lbImg = document.getElementById('jl-lightbox-img') as HTMLImageElement | null;
    if (dialog && lbImg) {
      document.querySelectorAll('.jl-screenshot').forEach(img => {
        img.addEventListener('click', () => {
          lbImg.src = (img as HTMLImageElement).src;
          lbImg.alt = (img as HTMLImageElement).alt;
          dialog.showModal();
        });
      });
      dialog.addEventListener('click', () => dialog.close());
    }

    // --- CTA tracking ---
    document.querySelector('.jl-store-link')?.addEventListener('click', () => {
      (window as any).dataLayer?.push({ event: 'joblens_cta_click' });
    });

    // --- Changelog ---
    const JL_CHANGELOG = [
      { version: 'v2.1',
        why: {
          en: 'Fewer clicks, more control. Mute and Unfollow now live right on every post.',
          zh: '更少点击，更多掌控。Mute 和 Unfollow 按钮现在直接显示在每条帖子上。',
        },
        changes: {
          en: [
            'Inline Mute & Unfollow buttons on every post — no more hunting for the ... menu',
            'Search your keyword rules from the popup',
            'Simplified popup: removed float toggles, split into clearer sections',
            'Narrowed job search scope to search results page for a more focused experience',
            'Fixed hidden cards not reappearing when toggling hideFiltered off',
          ],
          zh: [
            '每条帖子上直接显示 Mute 和 Unfollow 按钮，不用再找 ... 菜单',
            '弹窗中可搜索关键词规则',
            '简化弹窗：移除浮动开关，分区更清晰',
            '收窄求职页面范围至搜索结果页，体验更聚焦',
            '修复切换 hideFiltered 时隐藏卡片不重新显示的问题',
          ],
        }},
      { version: 'v2.0',
        why: {
          en: 'LinkedIn isn\'t just job search — your feed is half ads too. Why not clean up both?',
          zh: 'LinkedIn 不只有求职页面，你的 feed 也是一半广告。为什么不一起清理？',
        },
        changes: {
          en: [
            'Feed cleanup: hide ads, suggested, recommended posts; mute keywords; one-click unfollow',
            'Popup redesign: Controls, Stats, and Data tabs replace the floating panel',
            'Removed mute-person feature in favor of unfollow; keyword muting kept for spam filtering',
            'Stats tracking: daily and all-time counts for all filter actions',
          ],
          zh: [
            'Feed 清理：隐藏广告、推荐、建议帖子；关键词屏蔽；一键取消关注',
            '弹窗重新设计：Controls、Stats、Data 标签页取代浮动面板',
            '移除了屏蔽人功能，改用取消关注；保留关键词屏蔽用于过滤垃圾内容',
            '统计追踪：所有过滤操作的每日和累计计数',
          ],
        }},
      { version: 'v1.0',
        why: {
          en: 'LinkedIn search shows everything: reposted, applied, no-sponsor. Why not flag them automatically?',
          zh: 'LinkedIn 搜索什么都显示：已转发、已申请、不赞助签证。为什么不自动标记呢？',
        },
        changes: {
          en: [
            'Auto-scan LinkedIn job cards and flag Reposted, Applied, No Sponsor, Unpaid',
            'Skip lists for companies and title keywords, with batch import/export',
            'Draggable frosted-glass panel with dim mode for filtered cards',
          ],
          zh: [
            '自动扫描 LinkedIn 职位卡片，标记 Reposted、Applied、No Sponsor、Unpaid',
            '公司和职位关键词屏蔽列表，支持批量导入导出',
            '可拖动的毛玻璃面板，淡化模式让已标记卡片变暗',
          ],
        }},
    ];

    const clToggle = document.getElementById('jl-changelog-toggle');
    const clPopup = document.getElementById('jl-changelog-popup');
    const clBackdrop = document.getElementById('jl-changelog-backdrop');
    const clClose = document.getElementById('jl-changelog-close');
    const clContent = document.getElementById('jl-changelog-content');

    function renderChangelog() {
      if (!clContent) return;
      const lang = (document.documentElement.dataset.lang as 'en' | 'zh') || 'en';
      clContent.innerHTML = JL_CHANGELOG.map(r => `
        <div class="jl-changelog-release">
          <div class="jl-changelog-version">${r.version}</div>
          <p class="jl-changelog-why">${r.why[lang]}</p>
          <ul class="jl-changelog-list">
            ${r.changes[lang].map(c => `<li>${c}</li>`).join('')}
          </ul>
        </div>
      `).join('');
    }
    renderChangelog();
    document.addEventListener('lang-change', renderChangelog);

    function openChangelog() {
      if (clPopup) clPopup.style.display = '';
      if (clBackdrop) clBackdrop.style.display = '';
    }
    function closeChangelog() {
      if (clPopup) clPopup.style.display = 'none';
      if (clBackdrop) clBackdrop.style.display = 'none';
    }

    clToggle?.addEventListener('click', () => {
      if (clPopup?.style.display === 'none') openChangelog();
      else closeChangelog();
    });
    clBackdrop?.addEventListener('click', closeChangelog);
    clClose?.addEventListener('click', closeChangelog);

    // --- Feedback ---
    const toggle = document.getElementById('jl-feedback-toggle');
    const popup = document.getElementById('jl-feedback-popup');
    const backdrop = document.getElementById('jl-feedback-backdrop');
    const closeBtn = document.getElementById('jl-feedback-close');
    const form = document.getElementById('jl-feedback-form') as HTMLFormElement | null;
    const submitBtn = document.getElementById('jl-feedback-submit');
    const successMsg = document.getElementById('jl-feedback-success');
    const errorMsg = document.getElementById('jl-feedback-error');

    function openFeedback() {
      if (!popup || !backdrop) return;
      popup.style.display = '';
      backdrop.style.display = '';
      if (successMsg) successMsg.style.display = 'none';
      if (errorMsg) errorMsg.style.display = 'none';
      if (form) form.style.display = '';
    }

    function closeFeedback() {
      if (popup) popup.style.display = 'none';
      if (backdrop) backdrop.style.display = 'none';
    }

    toggle?.addEventListener('click', () => {
      if (popup?.style.display === 'none') openFeedback();
      else closeFeedback();
    });
    backdrop?.addEventListener('click', closeFeedback);
    closeBtn?.addEventListener('click', closeFeedback);

    form?.addEventListener('submit', async (e) => {
      e.preventDefault();
      if (!submitBtn || !form) return;
      submitBtn.setAttribute('disabled', '');
      submitBtn.textContent = '...';
      if (errorMsg) errorMsg.style.display = 'none';

      try {
        const res = await fetch('https://formspree.io/f/xreykpga', {
          method: 'POST',
          body: new FormData(form),
          headers: { Accept: 'application/json' },
        });
        if (!res.ok) throw new Error();
        form.style.display = 'none';
        if (successMsg) successMsg.style.display = '';
      } catch {
        if (errorMsg) errorMsg.style.display = '';
      } finally {
        submitBtn.removeAttribute('disabled');
      }
    });
  });
</script>

<style>
  .jl-header {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    margin-bottom: var(--space-xl);
  }

  .jl-logo {
    width: 64px;
    height: 64px;
    border-radius: 12px;
  }

  .jl-title-row {
    display: flex;
    align-items: baseline;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .jl-header h1 {
    font-size: var(--fs-xl);
    font-weight: 500;
    line-height: 1.2;
    color: var(--color-rose);
  }

  .jl-subtitle {
    font-size: 1rem;
    color: var(--text-light);
    margin-top: var(--space-xs);
  }

  /* Sections */
  .section {
    border-top: 1px solid var(--border);
    padding-top: var(--space-lg);
    margin-bottom: var(--space-xl);
  }

  .section h2 {
    font-size: var(--fs-lg);
    font-weight: 500;
    margin-bottom: var(--space-sm);
  }

  .section h3 {
    font-size: var(--fs-xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-light);
    margin-top: var(--space-md);
    margin-bottom: var(--space-xs);
  }

  .section p {
    color: var(--text-light);
    line-height: 1.6;
    margin-bottom: var(--space-xs);
  }

  /* Flag badges */
  .jl-flags {
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .jl-flag {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
  }

  .jl-flag-badge {
    font-size: 0.7rem;
    font-weight: 700;
    padding: 0.15em 0.6em;
    border-radius: 8px;
    color: #fff;
    white-space: nowrap;
    min-width: 5.5rem;
    text-align: center;
  }

  .jl-flag-desc {
    font-size: var(--fs-xs);
    color: var(--text-light);
  }

  /* Screenshots */
  .jl-screenshots {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-md);
  }

  @media (max-width: 640px) {
    .jl-screenshots {
      grid-template-columns: 1fr;
    }
  }

  .jl-screenshot-figure {
    margin: 0;
  }

  .jl-screenshot {
    width: 100%;
    border-radius: 8px;
    border: 1px solid var(--border);
    cursor: zoom-in;
    transition: opacity 0.2s ease;
  }

  .jl-screenshot:hover {
    opacity: 0.85;
  }

  .jl-screenshot-figure figcaption {
    font-size: var(--fs-xs);
    color: var(--text-light);
    text-align: center;
    margin-top: var(--space-xs);
  }

  /* Pipeline */
  .jl-pipeline {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    flex-wrap: wrap;
    margin-bottom: var(--space-md);
  }

  .jl-step {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.3rem;
  }

  .jl-step-icon {
    width: 2rem;
    height: 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-rose);
  }

  .jl-step-icon svg {
    width: 1.4rem;
    height: 1.4rem;
  }

  .jl-step-label {
    font-size: var(--fs-xs);
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text);
  }

  .jl-arrow {
    color: var(--text-light);
    font-size: var(--fs-md);
    margin-bottom: 1.2rem;
  }

  /* Steps */
  .jl-steps {
    padding-left: 1.5rem;
  }

  .jl-steps li {
    margin-bottom: var(--space-xs);
    line-height: 1.6;
    color: var(--text-light);
  }

  .jl-steps li::marker {
    color: var(--color-rose);
    font-weight: 600;
  }

  .jl-steps code {
    font-size: 0.85em;
    background: rgba(0, 0, 0, 0.04);
    padding: 0.1em 0.4em;
    border-radius: 3px;
  }

  .jl-steps strong {
    color: var(--text);
    font-weight: 600;
  }

  .jl-link {
    color: var(--color-rose);
    text-decoration: none;
    font-weight: 500;
  }

  .jl-link:hover {
    opacity: 0.7;
  }

  /* Features list */
  .jl-features {
    padding-left: 1.5rem;
    margin-top: var(--space-sm);
  }

  .jl-features li {
    margin-bottom: var(--space-xs);
    line-height: 1.6;
    color: var(--text-light);
  }

  .jl-features li::marker {
    color: var(--color-rose);
  }

  .jl-features strong {
    color: var(--text);
    font-weight: 600;
  }

  .jl-features code {
    font-size: 0.85em;
    background: rgba(0, 0, 0, 0.04);
    padding: 0.1em 0.4em;
    border-radius: 3px;
  }

  /* CTA */
  .jl-cta {
    text-align: center;
    margin-top: var(--space-xl);
    padding-bottom: var(--space-xl);
  }

  .jl-store-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5em;
    font-size: var(--fs-md);
    font-weight: 500;
    color: #fff;
    background: var(--color-rose);
    padding: 0.5em 1.2em;
    border-radius: 6px;
    text-decoration: none;
    transition: opacity 0.2s ease;
  }

  .jl-store-link:hover {
    opacity: 0.85;
  }

  .jl-store-icon {
    width: 1.2em;
    height: 1.2em;
  }


  /* Lightbox */
  .jl-lightbox {
    border: none;
    background: none;
    padding: 0;
    max-width: 90vw;
    max-height: 90vh;
    cursor: zoom-out;
  }

  .jl-lightbox::backdrop {
    background: rgba(0, 0, 0, 0.7);
  }

  .jl-lightbox img {
    max-width: 90vw;
    max-height: 90vh;
    border-radius: 8px;
    object-fit: contain;
  }

  /* Version badge & changelog */
  .jl-version-wrap {
    position: relative;
  }

  .jl-version-btn {
    font-family: inherit;
    font-size: var(--fs-xs);
    color: var(--text-light);
    background: none;
    border: 1px solid var(--border);
    border-radius: 3px;
    padding: 0.1em 0.4em;
    cursor: pointer;
    transition: border-color 0.15s, color 0.15s;
    white-space: nowrap;
  }

  .jl-version-btn:hover {
    border-color: rgba(0, 0, 0, 0.25);
    color: var(--text);
  }

  .jl-changelog-backdrop {
    position: fixed;
    inset: 0;
    z-index: 99;
  }

  .jl-changelog-popup {
    position: absolute;
    top: calc(100% + 0.4rem);
    left: 0;
    z-index: 100;
    width: min(22rem, calc(100vw - 2rem));
    max-height: 28rem;
    overflow-y: auto;
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 6px;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
    padding: 0.75rem;
  }

  @media (max-width: 480px) {
    .jl-changelog-popup {
      position: fixed;
      top: auto;
      right: 1rem;
      bottom: 1rem;
      left: 1rem;
      width: auto;
      max-height: 70vh;
    }
  }

  .jl-changelog-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.5rem;
  }

  .jl-changelog-title {
    font-size: var(--fs-xs);
    font-weight: 600;
    color: var(--text);
  }

  .jl-changelog-close {
    font-family: inherit;
    font-size: 1.1rem;
    color: var(--text-light);
    background: none;
    border: none;
    cursor: pointer;
    line-height: 1;
    padding: 0 0.15rem;
  }

  .jl-changelog-close:hover {
    color: var(--text);
  }

  .jl-changelog-popup :global(.jl-changelog-release) {
    margin-bottom: 0.6rem;
  }

  .jl-changelog-popup :global(.jl-changelog-release:last-child) {
    margin-bottom: 0;
  }

  .jl-changelog-popup :global(.jl-changelog-version) {
    font-size: var(--fs-xs);
    font-weight: 600;
    color: var(--text);
  }

  .jl-changelog-popup :global(.jl-changelog-why) {
    font-size: var(--fs-xs);
    color: var(--text-light);
    font-style: italic;
    margin: 0.15rem 0 0.2rem;
    line-height: 1.4;
  }

  .jl-changelog-popup :global(.jl-changelog-list) {
    margin: 0.2rem 0 0 1rem;
    padding: 0;
    font-size: var(--fs-xs);
    color: var(--text-light);
    line-height: 1.5;
  }

  .jl-changelog-popup :global(.jl-changelog-list li) {
    margin-bottom: 0.1rem;
  }

  /* Feedback button & popup */
  .jl-feedback-wrap {
    position: relative;
    display: inline-block;
  }

  .jl-github-btn {
    font-family: inherit;
    font-size: var(--fs-xs);
    color: var(--text-light);
    border: 1px solid var(--border);
    border-radius: 3px;
    padding: 0.1em 0.4em;
    text-decoration: none;
    white-space: nowrap;
    transition: border-color 0.15s, color 0.15s;
  }

  .jl-github-btn:hover {
    border-color: rgba(0, 0, 0, 0.25);
    color: var(--text);
  }

  .jl-feedback-btn {
    font-family: inherit;
    font-size: var(--fs-xs);
    color: var(--text-light);
    background: none;
    border: 1px solid var(--border);
    border-radius: 3px;
    padding: 0.1em 0.4em;
    cursor: pointer;
    transition: border-color 0.15s, color 0.15s;
    white-space: nowrap;
  }

  .jl-feedback-btn:hover {
    border-color: rgba(0, 0, 0, 0.25);
    color: var(--text);
  }

  .jl-feedback-backdrop {
    position: fixed;
    inset: 0;
    z-index: 99;
  }

  .jl-feedback-popup {
    position: absolute;
    top: calc(100% + 0.4rem);
    right: 0;
    z-index: 100;
    width: min(18rem, calc(100vw - 2rem));
    background: var(--bg);
    border: 1px solid var(--border);
    border-radius: 6px;
    box-shadow: 0 4px 16px rgba(0, 0, 0, 0.08);
    padding: 0.75rem;
  }

  @media (max-width: 480px) {
    .jl-feedback-popup {
      position: fixed;
      top: auto;
      right: 1rem;
      bottom: 1rem;
      left: 1rem;
      width: auto;
    }
  }

  .jl-feedback-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 0.5rem;
  }

  .jl-feedback-title {
    font-size: var(--fs-sm);
    font-weight: 500;
    color: var(--text);
  }

  .jl-feedback-close {
    font-family: inherit;
    font-size: 1.1rem;
    background: none;
    border: none;
    color: var(--text-light);
    cursor: pointer;
    padding: 0 0.2em;
    line-height: 1;
  }

  .jl-feedback-close:hover {
    color: var(--text);
  }

  .jl-feedback-textarea {
    width: 100%;
    font-family: inherit;
    font-size: var(--fs-sm);
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 0.5rem;
    resize: vertical;
    background: var(--bg);
    color: var(--text);
    box-sizing: border-box;
  }

  .jl-feedback-textarea:focus {
    outline: none;
    border-color: var(--text-light);
  }

  .jl-feedback-submit {
    margin-top: 0.5rem;
    font-family: inherit;
    font-size: var(--fs-sm);
    color: var(--text);
    background: none;
    border: 1px solid var(--border);
    border-radius: 4px;
    padding: 0.3em 0.8em;
    cursor: pointer;
    transition: border-color 0.15s;
  }

  .jl-feedback-submit:hover {
    border-color: rgba(0, 0, 0, 0.25);
  }

  .jl-feedback-submit:disabled {
    opacity: 0.5;
    cursor: default;
  }

  .jl-feedback-success {
    color: var(--color-ds-mid);
    font-size: var(--fs-sm);
    margin: 0.5rem 0 0;
  }

  .jl-feedback-error {
    color: var(--color-pm);
    font-size: var(--fs-xs);
    margin: 0.4rem 0 0;
  }
</style>
