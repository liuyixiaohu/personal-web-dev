---
import { professionalAreas } from '../../data/professionalAreas';

export function getStaticPaths() {
  return professionalAreas.map((area) => ({
    params: { slug: area.slug },
  }));
}

return Astro.redirect(`/${Astro.params.slug}`, 301);
---
